package com.ladera.SalesForceApplication.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/v2/salesforce/pricerow")
public class PricerowController {
//	@Autowired
//	private PricerowService pricerowService;

	/*
	 * @PostMapping("/createPricerow") public String createPrice(@RequestBody
	 * PricerowDTO pricerowDTO) { System.out.println("createpricerow");
	 * pricerowService.createPricerow(pricerowDTO); return
	 * "Product added successfully"; }
	 * 
	 * @GetMapping("/getAllPricerow") public List<Pricerow> getAllPricerow() {
	 * return pricerowService.getAllpricerow(); }
	 */
}
